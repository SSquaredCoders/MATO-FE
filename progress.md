Original prompt: Rebuild the StarCraft custom-map style song quiz web app. The old UI and websocket flow can be discarded if needed, and the new version can follow a modern stack and cleaner architecture.

- 2026-03-26: Audited the legacy frontend/backend pair. The current codebase mixes multiple abandoned frontend architectures, mismatched REST routes, and mismatched websocket event names.
- 2026-03-26: Started a frontend v2 scaffold in `src/` with a modern app shell, route map, query provider, and a lightweight room-session store for the next implementation pass.
- 2026-03-26: Added a shared v2 architecture document in the backend repo to lock down the API, realtime contract, and migration phases before server changes begin.
- 2026-03-26: Installed Node LTS and Temurin JDK 21 with winget.
- 2026-03-26: Added local Spring Boot defaults for H2, Redis, and JWT so `gradlew test` passes and `bootRun` starts without prod-only environment variables.
- 2026-03-26: Verified the frontend locally with `npm install`, `npm run build`, Vite preview, and a Playwright smoke flow that covers lobby load, room entry, ready toggle, game start, correct answer submit, score increment, and round advance.
- 2026-03-26: Cleaned up stray mojibake in the local demo UI and tightened the demo store typing around the room host participant.
- 2026-03-26: Implemented the backend v2 room runtime under `/api/v2` with a fresh in-memory room/game service, `GET /api/v2/lobby/rooms`, `POST /api/v2/rooms`, `GET /api/v2/rooms/{roomName}`, and a single STOMP endpoint at `/ws/game`.
- 2026-03-26: Replaced the temporary frontend demo room store with live REST reads plus a single room websocket hook, keeping only a small session store for the acting nickname.
- 2026-03-26: Re-ran end-to-end verification against the live backend. The browser flow now reaches `connected`, enters `demo-room`, toggles ready, starts the game as the host, accepts a correct answer, and advances to round `2/4`.
- 2026-03-26: Adjusted answer flow so wrong answers no longer overwrite the main feedback text, while correct answers reveal the actual song/artist in the transient chat, keep the reveal on screen, and advance immediately to the next round. Re-verified with a two-session browser flow and captured `output/web-game/answer-flow-check.png`.
- 2026-03-26: Reworked the room layout around the Now Playing stage. The right sidebar now centers on ready/start/leave actions, the ready button changes state visually, the start button is host-only, and secondary test controls moved into a collapsed `details` block.
- 2026-03-26: Added a first in-memory v2 map catalog on the backend with seeded maps plus `GET /api/v2/maps`, `GET /api/v2/maps/{id}`, and `POST /api/v2/maps`.
- 2026-03-26: Replaced the placeholder maps page with a live map catalog/detail/create flow and wired lobby room creation so a selected map becomes the room's song set.
- 2026-03-26: Verified the map slice end-to-end by creating a map in the browser, selecting it in the lobby, creating a room, and confirming the room snapshot reports the selected map and correct total round count. Captured `output/web-game/map-room-flow-maps.png`, `output/web-game/map-room-flow-room.png`, and `output/web-game/map-room-flow.json`.
- 2026-03-26: Added map-level hint reveal timing. Maps now store `hintRevealDelaySeconds`, room snapshots expose `currentHint` plus `hintRevealAt`, and the room UI hides the hint until the reveal time and shows a countdown first.
- 2026-03-26: Re-verified the hint timing slice against a live backend by creating a 4-second hint-delay map, starting a room over the STOMP channel, and confirming the room board shows a countdown first and then reveals the hint. Captured `output/web-game/hint-delay-before.png`, `output/web-game/hint-delay-after.png`, and `output/web-game/hint-delay-flow.json`.

TODO
- Persist the v2 room runtime outside of process memory once the contract is stable.
- Decide whether lobby updates also need a dedicated realtime channel or if query invalidation is enough.
- Add update/delete flows for maps and decide when room rule overrides live on the map versus on the room.
- Bring media ingestion and actual audio playback back on top of the stable map/game loop.

2026-03-26 17:56 - Added owner-only map filtering plus song source inputs for YouTube URLs and uploaded audio files. Verified guest-77 map stays hidden for host-01, created a mixed-source private map in the browser, and confirmed lobby map selection only shows the owner's map.
2026-03-27 10:35 - Split the maps page into three explicit modes: overview, edit, and create. The overview now shows only the map list plus selected-map detail, edit shows the user's map list alongside the editor, and create gives the map builder a wider main column with a narrower added-songs side list. Verified the three states with Playwright screenshots at `output/web-game/maps-overview-check/shot-0.png`, `output/web-game/maps-edit-check/shot-0.png`, and `output/web-game/maps-create-check/shot-0.png`.
2026-03-27 13:15 - Switched the clip timing rule away from per-song time overrides. Maps now default to showing the playback UI, rounds extend automatically when a configured clip is longer than the base round time, and shorter clips are expected to loop from their start point during playback. Reworked the map builder so clip start/end keep numeric inputs and also expose range sliders, then verified the create screen with `output/web-game/maps-clip-slider-full.png`.
2026-03-27 13:44 - Added a builder-only clip preview player so map creation/editing always shows playable media while tuning the clip range, independent of the in-game media visibility toggle. Verified with a live browser upload flow and captured `output/web-game/maps-create-preview-player.png`.
2026-03-27 14:08 - Replaced the builder's native file audio control with a custom preview transport that supports play/pause, 3-second nudges, scrubbing, and pushing the current playhead into the clip start/end fields. Verified the create screen with a real wav upload and captured `output/web-game/maps-custom-preview-controls.png`.
2026-03-27 14:34 - Added the same single-bar clip editor to YouTube-link songs by simplifying the preview to a real embedded YouTube player plus the shared start/end timeline. Verified with `npm run build`, the web-game Playwright client, and a browser flow that filled a YouTube URL in create mode and captured `output/web-game/maps-youtube-clip-editor.png`.
2026-03-30 09:45 - Reworked the YouTube builder preview so the iframe stays mounted and clip edits no longer rebuild the video URL on every slider change. The preview now uses a stable embed URL with `enablejsapi=1` and sends `seekTo` commands back into the existing iframe for start-point preview updates. Verified with `npm run build` and a Playwright browser check that the preview remained the same iframe node with the same `src` after slider interaction, captured in `output/web-game/maps-youtube-live-preview.json` and `output/web-game/maps-youtube-live-preview.png`.
2026-03-30 10:10 - Expanded the YouTube builder branch to show the same transport actions as file previews and wired `ÌòÑÏû¨ ÏúÑÏπòÎ•º ÏãúÏûëÏ†êÏúºÎ°ú` through the live preview clock. Verified with `npm run build` and a browser flow that reached `afterCapture.startInput = 1` in `output/web-game/maps-youtube-controls-check.json`, with the matching screenshot at `output/web-game/maps-youtube-controls-check.png`. The remaining rough edge is that headless YouTube autoplay still behaves inconsistently, so manual browser verification is still recommended for final feel.
2026-03-30 10:45 - Cleaned up the map builder preview copy after the YouTube controls pass. The clip timeline labels, transport buttons, guide copy, and iframe title/fallback text in `MapsPage.tsx` are readable Korean again, and the map create helper text now correctly says the default is `«√∑π¿ÃæÓ «•Ω√`. Re-verified with `cmd /c npm run build`, the web-game Playwright client smoke run, and a browser flow that captured `output/web-game/maps-youtube-preview-text-check.png` plus `output/web-game/maps-helper-matches.json`.
2026-03-30 11:36 - Added `scripts/verify-room-clip-range.mjs` plus the `verify:room-clip-range` npm script so the room playback path can be checked end-to-end. The script creates a temporary map with a YouTube clip range, creates a room through the lobby UI, joins a second participant over a separate raw STOMP websocket session, starts the game, and asserts the room iframe plus snapshot both carry `start=11` and `end=22`. Verified with `cmd /c npm run verify:room-clip-range`, which wrote `output/web-game/room-clip-range-check.json` and `output/web-game/room-clip-range-check.png`.
2026-03-30 11:45 - Fixed the room debug `∞¸¿¸¿⁄ √ﬂ∞°` flow so it no longer steals the main websocket session. `RoomPage.tsx` now opens a dedicated STOMP client per debug watcher and keeps the main player intact, which means the room stays alive and the extra participant appears in the control dropdown. Re-verified with `cmd /c npm run build`, a browser flow captured in `output/web-game/room-watcher-check.json` / `output/web-game/room-watcher-check.png`, and a fresh `cmd /c npm run verify:room-clip-range` regression run.
2026-03-30 12:01 - Rebalanced the room board typography after the waiting prompt grew too aggressively. Reduced the clean-stage headline clamp, loosened the line height slightly, and widened the clean-stage measure so long Korean strings stop overwhelming the board. Re-verified with cmd /c npm run build and a live browser capture at output/web-game/room-font-size-check.png.
2026-03-30 12:18 - Reworked the room-stage transient chat into a dedicated left-hand lane instead of an absolute overlay on top of the prompt. Increased the visible stack to four messages, stretched the cards for readability, extended the lifetime to 6 seconds, and re-verified with cmd /c npm run build plus a live browser capture at output/web-game/room-chat-lane-check.png.
2026-03-30 12:26 - Reduced the transient chat card typography so longer player messages stay readable inside the new left-hand lane. Kept the larger cards and dedicated lane, then re-verified with cmd /c npm run build and an injected browser capture at output/web-game/room-chat-font-size-check.png.
2026-03-30 12:34 - Reduced the main room prompt typography again after the player-facing question copy still felt oversized inside the stage. Lowered the base, clean-stage, and mobile clamps together, then re-verified with cmd /c npm run build and a live browser capture at output/web-game/room-prompt-font-size-check.png.
2026-03-30 12:41 - Reduced the main room prompt typography one more step after the question text still felt oversized in the stage. Lowered the base and clean-stage clamps together again, widened the measure slightly, and re-verified with cmd /c npm run build plus output/web-game/room-prompt-font-size-check-2.png.
2026-03-30 12:53 - Fixed the stage eyebrow so Now Playing stays pinned at the top of the board even when the prompt grows, and added a ¥Î±‚πÊ ªı∑Œ∞Ìƒß button to the room sidebar during the lobby phase. Re-verified with cmd /c npm run build and a live browser capture at output/web-game/room-header-refresh-check.png.
2026-03-30 13:04 - Moved the room-list refresh action out of the room sidebar and into the lobby, which matches the user flow better. The room board eyebrow also stays pinned above the prompt now. Re-verified with cmd /c npm run build and output/web-game/lobby-refresh-button-check.png.
2026-03-30 15:43 - Added map-level song order modes (¡¶¿€¿⁄ º¯º≠ / ∑£¥˝) across the frontend and backend. Maps now persist the chosen mode, the room runtime reorders songs at game start based on it, and the map editor exposes both the mode selector and per-song up/down controls for authored ordering. Re-verified with cmd /c gradlew.bat test, cmd /c npm run build, and a browser capture at output/web-game/maps-song-order-mode-check.png.
2026-03-31 09:18 - Reworked the maps route into a fixed-height workbench shell. The /maps route now uses a dedicated compact app shell, the page itself stays locked to the viewport, and map overview/create flows scroll inside panels instead of pushing the whole document. Re-verified with 
pm run build, the develop-web-game Playwright client capture at output/web-game/map-workbench-client-2/shot-0.png, and a browser check that document.body.scrollHeight === document.documentElement.clientHeight.
2026-03-31 10:38 - Improved high-volume map editing on /maps. Added denser song rows for overview/editor queues, kept queue scrolling confined to the list, made selected songs page into view automatically, and added a bulk import tool that accepts tab- or pipe-delimited rows from spreadsheets. Verified with npm run build plus Playwright captures at output/web-game/map-dense-queue.png and output/web-game/map-bulk-import.png.
2026-03-31 11:12 - Added spreadsheet-driven map import. The bulk import tool on /maps now offers an Excel template download plus .xlsx/.xls/.csv upload, parses the first sheet client-side, and appends imported songs to the current map draft. Switched xlsx loading to dynamic imports so the main frontend bundle stays lean. Verified with npm run build and a browser upload flow captured at output/web-game/map-excel-import.png.
2026-03-31 11:29 - Finished the spreadsheet round-trip flow for map songs. The bulk import panel now downloads a blank Excel template, exports the current draft to XLSX, and imports .xlsx/.xls/.csv files in either append or replace mode. Verified with npm run build plus a browser round-trip that captured downloads named mato-map-import-template.xlsx / mato-map-songs.xlsx and a replace import ending with 2 songs in output/web-game/map-excel-roundtrip.png.
2026-03-31 11:46 - Made the spreadsheet import flow more self-guided. Added an 8-column on-screen guide with required/optional labels plus examples, and surface a post-import summary banner so users can immediately see whether songs were appended or replaced. Verified with npm run build and the browser capture at output/web-game/map-import-guide.png.
2026-03-31 11:54 - Added a confirmation guard before spreadsheet-based replace imports. The bulk import flow now asks before replacing the existing draft, and dismissing the dialog leaves the current song list untouched. Re-verified with npm run build and a browser flow that captured the confirm text while keeping the queue count unchanged.
2026-03-31 12:03 - Made spreadsheet imports header-driven. XLSX/CSV imports now map columns by header name (with English/Korean aliases) instead of relying on fixed column order, while still supporting the original template order when no header row is present. Re-verified with npm run build plus a browser import of a reordered-column XLSX that replaced the draft with Again / Silhouette and captured output/web-game/map-import-reordered-columns.png.
2026-03-31 14:20 - Finished the map editor wrap-up pass. Added unsaved-change tracking, beforeunload protection, confirmation prompts before leaving create/edit drafts, a visible dirty-state chip in the builder header, and success feedback banners after create/update/delete flows. Re-verified with npm run build plus a Playwright browser flow captured in output/web-game/map-guard-feedback-check.png and output/web-game/map-guard-feedback-check.json.
2026-03-31 13:56 - Added configurable skip-vote rules across the map editor and live room flow. Maps now store skipVotesRequired, the room runtime exposes live skip vote counts and voter nicknames, the room UI turns the old host-only skip into a vote button for timer-or-skip rounds, and the map overview/editor surfaces the configured skip threshold. Re-verified with cmd /c gradlew.bat test, cmd /c npm run build, and a browser flow that created a 2-vote map, started a room, cast two skip votes, and captured output/web-game/room-skip-vote-check.png plus output/web-game/room-skip-vote-check.json.

2026-04-01 11:05 - Slimmed the frontend shell for beta readability and mobile use. Flattened the visual system in src/index.css by removing heavy blur/glass effects, reducing gradients/shadows/radii, tightening room/map panels, and stacking the map workbench earlier on smaller screens. Re-verified with cmd /c npm run build plus desktop/mobile browser captures at output/web-game/maps-desktop-slim-check-2.png and output/web-game/maps-mobile-slim-check-2.png.
2026-04-01 11:05 - Added route-level lazy loading in src/App.tsx so RoomPage, MapsPage, RoadmapPage, and AccountPage no longer inflate the initial lobby bundle. The main chunk dropped from roughly 375 kB to 278 kB while maps/room now load on demand.
2026-04-01 11:05 - Removed the lingering host-01 fallback from the map editor flow. MapsPage.tsx now derives the viewer/creator nickname from the actual session or the visible nickname input, blocks map save when the nickname is still blank, and exposes a simple top-of-screen nickname field for beta testers.
2026-04-03 10:40 - Revived the v2 auth flow on top of the existing JWT backend instead of introducing a new auth stack. Backend now issues nickname-aware JWTs, exposes `GET /api/users/me` and `POST /api/users/logout`, and falls back to an in-memory refresh-token cache when local Redis is unavailable so login/register works in dev without extra setup.
2026-04-03 10:40 - Added a lightweight frontend auth layer with persisted session state, automatic token/header reuse, boot-time current-user restore, and a real account screen for login/register/logout. Verified the browser flow by registering a fresh account and reaching `/maps`, with artifacts at `output/web-game/auth-maps-flow-check.png` and `output/web-game/auth-maps-flow-check.json`.
2026-04-03 10:40 - Wired the lobby/maps/room flows back into authenticated users. Lobby now uses the logged-in nickname automatically when available, the maps route is gated behind login and no longer relies on manual author entry, and the room page seeds its acting nickname from the authenticated session on direct entry.
TODO
- Remove the now-hidden map nickname field from `MapsPage.tsx` entirely instead of leaving it CSS-hidden. The current behavior is correct, but the JSX still carries a read-only field.
- Decide whether room creation/join should eventually be auth-only or keep the current guest fallback in the lobby.
- If production keeps Redis, consider making the refresh-token fallback profile-specific so non-dev deployments fail loudly instead of silently using memory.
